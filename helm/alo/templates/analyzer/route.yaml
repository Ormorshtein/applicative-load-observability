{{- if and .Values.analyzer.enabled .Values.route.enabled .Values.route.analyzer.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "alo.fullname" . }}-analyzer
  labels:
    {{- include "alo.labels" . | nindent 4 }}
    {{- include "alo.selectorLabels" (dict "ctx" . "component" "analyzer") | nindent 4 }}
  {{- include "alo.routeAnnotations" (dict "global" .Values.route.annotations "local" .Values.route.analyzer.annotations) | nindent 2 }}
spec:
  {{- if .Values.route.analyzer.host }}
  host: {{ .Values.route.analyzer.host }}
  {{- end }}
  to:
    kind: Service
    name: {{ include "alo.fullname" . }}-analyzer
    weight: 100
  port:
    targetPort: http
  {{- include "alo.routeTls" (dict "global" .Values.route.tls "local" .Values.route.analyzer.tls) | nindent 2 }}
{{- end }}
