{{- if and .Values.ingress.enabled (not .Values.elasticsearch.external.enabled) .Values.ingress.elasticsearch.host }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "alo.fullname" . }}-elasticsearch
  labels:
    {{- include "alo.labels" . | nindent 4 }}
    {{- include "alo.selectorLabels" (dict "ctx" . "component" "elasticsearch") | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.elasticsearch.host }}
      http:
        paths:
          - path: {{ .Values.ingress.elasticsearch.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "alo.fullname" . }}-elasticsearch
                port:
                  name: http
{{- end }}
