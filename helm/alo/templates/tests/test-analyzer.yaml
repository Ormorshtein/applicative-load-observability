apiVersion: v1
kind: Pod
metadata:
  name: {{ include "alo.fullname" . }}-test-analyzer
  labels:
    {{- include "alo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  {{- with .Values.imagePullSecrets }}
  imagePullSecrets:
    {{- include "alo.imagePullSecrets" $ | nindent 4 }}
  {{- end }}
  containers:
    - name: test-analyzer
      image: "{{ .Values.testImage.repository }}:{{ .Values.testImage.tag }}"
      command: ["sh", "-c"]
      args:
        - |
          echo "Testing analyzer connectivity..."
          wget -qO- --timeout=10 http://{{ include "alo.fullname" . }}-analyzer:{{ .Values.analyzer.port }}/docs || exit 1
          echo "Analyzer is reachable."
