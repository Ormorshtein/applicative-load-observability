apiVersion: 1

datasources:
  - name: Prometheus (ALO)
    type: prometheus
    uid: alo-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    jsonData:
      httpMethod: POST
      timeInterval: "15s"
    editable: true
